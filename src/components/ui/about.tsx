import Image from "next/image";
import "../../app/font.css"

export const About = () => {
    return(
        <section className="bg-white w-full flex justify-center items-center py-20">
            
        </section>
    );
}